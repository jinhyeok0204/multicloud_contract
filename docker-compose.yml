version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - SQLALCHEMY_DATABASE_URI=${SQLALCHEMY_DATABASE_URI}
      - UPLOAD_FOLDER=${UPLOAD_FOLDER}
    volumes:
      - .:/app
    env_file:
      - .env